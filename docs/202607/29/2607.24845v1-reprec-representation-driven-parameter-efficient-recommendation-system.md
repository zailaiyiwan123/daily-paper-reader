---
title: "REPREC: Representation Driven Parameter-Efficient Recommendation System"
title_zh: REPREC：表示驱动的参数高效推荐系统
authors: "Harshini Kavuru, Dwipam Katariya, Giri Iyengar, Pranab Mohanty, Kalanand Mishra, Kalanand Mishra"
date: 2026-07-24
pdf: "https://arxiv.org/pdf/2607.24845v1"
tags: ["query:pers-gen"]
score: 8.0
evidence: 基于LLM的推荐中用户个性化
tldr: "现有基于LLM的序列推荐常需微调、附加模块或处理长交互历史，训练与部署成本高。REPREC提出轻量框架，通过冻结序列编码器和LLM，仅训练MLP注入器将固定大小用户嵌入映射为软token，实现参数高效推荐。实验表明，REPREC一致优于LoRA，尤其在低数据场景下效果显著，且训练时间平均减少1.51倍，保持85-100%性能，兼顾质量与效率。该工作为生产部署提供模块化、低成本的推荐方案。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.24845v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1407, \"height\": 685, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.24845v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1411, \"height\": 397, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.24845v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1547, \"height\": 341, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.24845v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1550, \"height\": 558, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.24845v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1573, \"height\": 310, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.24845v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1568, \"height\": 422, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.24845v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1566, \"height\": 328, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.24845v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 862, \"height\": 449, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.24845v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 677, \"height\": 316, \"label\": \"Table\"}]"
motivation: 现有LLM推荐方法依赖微调或复杂模块，训练部署成本高，需更参数高效的方案。
method: 冻结序列编码器和LLM，用MLP注入器将用户嵌入映射为软token条件化LLM，仅训练注入器。
result: "一致优于LoRA，低数据用户增益明显；短提示训练长上下文评估保持85-100%性能，训练提速1.51倍。"
conclusion: 提出轻量模块化推荐框架，无需修改预训练组件，平衡质量与效率，适合生产部署。
---

## 摘要
大型语言模型（LLM）已被应用于序列推荐，通过将其表述为自然语言任务。先前的工作通过输入条件设置或LLM微调，整合协同信号和序列信号来改进个性化。然而，现有方法通常依赖于以下一种或多种：LLM微调、额外的架构模块、表示蒸馏或对长交互历史的项目级条件设置，从而增加了训练复杂性和部署成本。我们提出REPREC，一个轻量级框架，通过轻量级用户表示对齐来重新表述基于LLM的序列推荐。REPREC通过一个轻量级MLP注入器，将来自冻结的序列编码器的固定大小用户嵌入映射到一组学习到的软标记中，从而条件化一个冻结的LLM，保持两个预训练骨干网络不变，仅训练注入器。我们在多个基准数据集上进行了详尽的实验，并证明REPREC在保持与不同预训练序列编码器和LLM骨干网络兼容的同时，始终优于LoRA，无需修改任一预训练组件即可实现模块化且适合生产的推荐流水线。这种提升在所有数据集的偶然用户和核心用户中尤为显著，突显了REPREC在低数据场景下的有效性。最后，当在短提示历史下训练并在更长上下文中评估时，REPREC保持了LoRA性能的85-100%，同时每轮训练时间平均减少1.51倍，展示了在生产部署中推荐质量与计算效率之间的有效平衡。代码可在https://github.com/phdbotcode/REPREC获取

## Abstract
Large language models (LLMs) have been applied to sequential recommendation by formulating it as a natural language task. Previous work has improved personalization by incorporating collaborative and sequential signals through input conditioning or LLM fine-tuning. However, existing approaches often rely on one or more of the following: LLM fine-tuning, additional architectural modules, representation distillation, or item-level conditioning over long interaction histories, increasing training complexity and deployment cost. We propose REPREC, a lightweight framework that reformulates LLM-based sequential recommendation through lightweight user representation alignment. REPREC maps a fixed-size user embedding from a frozen sequential encoder into a small set of learned soft tokens through a lightweight MLP injector that conditions a frozen LLM, leaving both pretrained backbones unchanged while training only the injector. We conducted exhaustive experiments on multiple benchmark datasets and demonstrate that REPREC consistently outperforms LoRA while remaining compatible with different pretrained sequential encoders and LLM backbones, enabling a modular and production-friendly recommendation pipeline without modifying either pretrained component. The gains are particularly pronounced for casual and core users across all datasets, highlighting REPREC's effectiveness in low-data regimes. Finally, when trained on short prompt histories and evaluated with longer contexts, REPREC maintains 85-100% of LoRA's performance while reducing per-epoch training time by an average of 1.51X, demonstrating an effective balance between recommendation quality and computational efficiency for production deployment. The code is available at https://github.com/phdbotcode/REPREC

---

## 论文详细总结（自动生成）

# REPREC：表示驱动的参数高效推荐系统——论文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：如何在不微调大型语言模型（LLM）或修改其架构的前提下，将序列推荐中的协同信号有效地注入LLM，以实现高效、模块化的推荐系统。现有基于LLM的推荐方法往往依赖LLM微调（如LoRA）、额外架构模块、表示蒸馏或项目级条件设置，导致训练复杂度和部署成本高，且不利于生产环境集成。
- **研究动机**：实际推荐系统除推荐质量外，还需考虑推理延迟、训练速度、可扩展性和部署简便性。作者探索“完全冻结”的范式：即冻结序列编码器和LLM，仅通过一个轻量的用户级表示对齐模块连接二者，从而实现参数高效、模块化且兼容多种骨干网络的推荐流水线。
- **整体含义**：REPREC证明了仅用少量可训练参数（MLP注入器），通过将固定大小的用户嵌入映射为软token来条件化冻结的LLM，就能在多数场景下超越LoRA等主流方法，尤其对低数据用户（casual/core user）提升显著，并在训练效率上取得优势。

## 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：借鉴多模态领域的软prompt方法（如CLIPCap、BLIP-2），将用户表示对齐到LLM的嵌入空间，而不修改任何预训练骨干。
- **框架组件**（如图1所示）：
  1. **序列编码器** \(f_\theta\)：使用传统的推荐模型（如SASRec、BERT4Rec）在用户交互序列上训练后冻结，输出固定维度的用户嵌入 \( \mathbf{u} \in \mathbb{R}^d \)。
  2. **注入器** \(g_\phi\)：一个轻量级MLP，将用户嵌入映射为 \(m\) 个软token（维度为LLM隐藏大小 \(D\)）。具体公式：
     \[
     g_\phi(\mathbf{u}) = \text{LN}_D\big(\text{reshape}_{m \times D}(\mathbf{W}_2\sigma(\mathbf{W}_1\mathbf{u}+\mathbf{b}_1)+\mathbf{b}_2)\big)
     \]
     其中 \(\sigma\) 为GELU激活，LN为LayerNorm。输出 \(Z \in \mathbb{R}^{m \times D}\) 作为软条件token。
  3. **冻结的LLM** \(M\)：接收输入 \([Z; E_{\text{prompt}}]\)，其中文本提示包含候选物品的二分类问题（“是否推荐该物品？”）。LLM输出下一个token的logits，仅取答案位置（Yes/No）的softmax概率，计算交叉熵损失。
- **训练**：仅更新注入器参数 \(\phi\)，梯度通过冻结的LLM回传。整个过程中序列编码器和LLM均保持冻结。
- **关键细节**：
  - 注入器输出经LayerNorm后与文本prompt拼接，作为LLM输入。
  - 使用二元决策prompt（每个候选物品生成一个“是否推荐”问题），而非直接生成物品名称。
  - 用户表示是固定大小的，不随交互历史长度增长，从而避免输入序列变长导致的FLOPs增加。

## 3. 实验设计：数据集、基准、对比方法

- **数据集**：使用Amazon 2018版五个产品评论数据集：Beauty、Sports & Outdoors、Toys & Games、Pet Supplies、Tools & Home Improvement。预处理后每个用户最后一个交互作为测试，倒数第二个作为验证。数据集高度稀疏（平均序列长度7.95-8.88），用户分为casual（0-5交互）、core（6-20）、power（>20）。
- **基准**：
  - 传统序列推荐：SASRec、BERT4Rec。
  - 基于LLM的基线：Zero-shot LLM（仅文本历史）、Frozen Injector（随机初始化注入器冻结）、LoRA-FT（rank=8，匹配参数预算）、LLaRA（项目级投影，使用SASRec/BERT4Rec作为编码器）。LLM骨干初始为LLaMA-3.2-3B-Instruct，也使用Qwen验证泛化性。
- **对比方法**：REPREC使用两种序列编码器变体（SASRec、BERT4Rec），两种LLM骨干（LLaMA、Qwen），与对应配置的基线对比。

## 4. 资源与算力

- 论文明确提到训练时间测量：使用NVIDIA A100 (40GB) GPU，每epoch训练时间在表3中给出（例如Beauty: 430分钟/epoch for REPREC, 314分钟/epoch for LoRA）。但未说明GPU数量、具体训练轮数或总训练时长。
- 可推断实验环境为单卡或少数A100，规模适中。

## 5. 实验数量与充分性

- **实验组数**：主实验（表2）报告了5个数据集、3种LLM方法（LoRA-FT、REPREC-S、REPREC-B）各5次随机种子的均值和标准差，覆盖两个LLM骨干。另外进行了以下消融和分析：
  - 参数预算对齐实验（图2）：变化软token数量 \(m\) 和LoRA rank \(r\)，比较性能与参数量关系。
  - 用户群体分析（图3、4）：分casual、core、power用户比较性能。
  - 训练/推理prompt长度匹配实验（表3、图4）：训练时用短历史（ℓ=10），测试时用长历史（ℓ=50），分析性能恢复及训练加速。
  - 负采样策略（附录B）：使用混合随机+流行度负采样（200负例），确保公平。
- **充分性与公平性**：
  - 采用了匹配参数预算（LoRA rank=8与REPREC m=6参数量接近），对比公平。
  - 所有LLM方法使用相同的LLM骨干和训练数据。
  - 报告多次运行的均值和标准差，评估稳定性。
  - 消融实验覆盖了关键组件（注入器随机、无历史prompt等）。
  - 不足：未与更多最新LLM推荐方法（如User-LLM、CoLLM等）对比；只在Amazon五个稀疏数据集上验证，未涉及长历史或更大规模数据集（如MovieLens-20M、Steam）。另外，未对比LLaRA的完整训练设置（LLaRA可能需要更多参数）。

## 6. 论文的主要结论与发现

- REPREC在所有数据集上一致超越或匹配LoRA，且无需微调LLM。如Beauty上HIT@10：REPREC-S 0.450 vs LoRA 0.435。
- 对casual和core用户提升尤为明显（HIT@10相对提升1.17%-10.86%），说明在低数据场景下，协同表示能有效补充语义信息。
- 使用短prompt历史（ℓ=10）训练，长历史（ℓ=50）测试，REPREC可恢复LoRA 85-100%的性能，同时每epoch训练时间减少平均1.51倍（如Toys提速1.81倍）。这验证了其“训练短、推理长”的高效策略。
- 少量软token（如m=2）即能带来显著改善，且增加m边际效益递减。
- 文本历史与注入的表示互补：纯注入无文本（prompt=0）性能远低于包含历史文本的配置。

## 7. 优点

- **参数效率**：仅训练约2.4M参数（m=6），远少于LoRA（2.29M）但匹配；无需修改骨干网络，模块化程度高。
- **生产友好**：保持编码器和LLM独立，易于替换或升级；训练时间短，推理时输入序列长度恒定（仅m个软token+短文本），降低延迟。
- **泛化能力**：可兼容不同序列编码器（SASRec、BERT4Rec）和不同LLM（LLaMA、Qwen），证明了通用性。
- **充分消融**：对用户细分、参数量、prompt长度等进行了深入分析，结论扎实。
- **代码开源**：提供GitHub仓库，增强可复现性。

## 8. 不足与局限

- **实验规模有限**：仅五个Amazon稀疏数据集，平均序列长度<10，未在长历史（如数十或上百交互）数据集上验证；也未在更大规模（如百万用户）或工业级数据集上测试，可能无法全面反映实际生产环境。
- **对比方法覆盖不全**：仅与LoRA、LLaRA比较，未与同样冻结LLM的方法（如LLM-SRec、SFT-LM等）或更复杂的端到端方法（如CoLLM、User-LLM）对比。此外，未与微调整个LLM的方法（全参数微调）比较，但参数量预算不同也许不公平。
- **未报告推理效率**：虽计算了训练时间，但未给出推理延迟或吞吐量，对于生产部署关键指标缺失。
- **负采样策略可改进**：虽然使用了混合负采样，但仅200个负例，且固定采样集可能引入偏差；未来可用在线采样或更复杂的策略。
- **注入器设计简单**：MLP注入器可能对更长交互历史或更复杂用户行为模式表达能力有限；未探索更复杂的对齐方法（如交叉注意力、门控机制）。
- **对power用户优势有限**：在交互频繁的用户上，REPREC与LoRA差距缩小甚至略逊（如Beauty），说明长文本历史本身已能提供足够信息，注入表示的作用减弱。
- **跨域迁移未研究**：未考察预训练序列编码器在不同领域间的迁移能力或注入器的泛化性。

（完）
