---
title: Generating Personalized Lower-Limb Kinematics Across Walking Speeds Using Subject-Conditioned Diffusion
title_zh: 使用受试者条件扩散模型生成跨步行速度的个性化下肢运动学
authors: "Diya Dinesh, Adrian Krieger, Changseob Song, Dongho Park, Aaron J. Young, Inseung Kang"
date: 2026-07-08
pdf: "https://arxiv.org/pdf/2607.07533v1"
tags: ["query:pers-gen"]
score: 8.0
evidence: 通过受试者条件扩散生成个性化下肢运动学
tldr: "个性化外骨骼辅助需要用户特定步态数据，但跨速度采集成本高、负担重。本文提出主题条件残差扩散框架，从单个可见速度的步态序列生成未见速度下的下肢运动学。在健全受试者上MAE为3.4°，在未微调的卒中受试者上为6.0°，误差相比监督基线降低超70%。单速度精度与四速度相当，显著减少了数据采集负担，为外骨骼个性化提供高效方案。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.07533v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1302, \"height\": 762, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.07533v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 891, \"height\": 550, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.07533v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 896, \"height\": 957, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.07533v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 899, \"height\": 324, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.07533v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 905, \"height\": 412, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.07533v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 888, \"height\": 410, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.07533v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 905, \"height\": 616, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.07533v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 617, \"height\": 199, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.07533v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 761, \"height\": 279, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.07533v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 777, \"height\": 164, \"label\": \"Table\"}]"
motivation: 跨速度个性化步态数据采集耗时费力，尤其对临床人群负担重，阻碍外骨骼个性化应用。
method: 采用主题条件残差扩散模型，通过Transformer去噪器和特征线性调制，生成从可见速度到目标速度的运动学残差。
result: "健全受试者MAE 3.4°，卒中受试者6.0°，误差降低超70%；单速度匹配四速度精度在0.4°内。"
conclusion: 该方法从极少量数据合成个性化跨速度步态，有效降低数据采集成本，助力外骨骼个性化适配。
---

## 摘要
个性化外骨骼辅助需要用户在不同运动任务下的步态数据，然而收集这些数据需要重复的动作捕捉过程，成本高、耗时，且对临床人群尤其困难。这一挑战在步行速度变化时最为突出，因为步态会显著改变，并在临床步态中偏离更大。本文提出了一种受试者条件残差扩散框架，该框架能够从单个已知速度下的受试者步态序列，生成未见过的步行速度下的个性化下肢运动学。给定已知速度下的矢状面髋、膝、踝轨迹以及所需的新速度，模型生成一个残差，将已知轨迹转换为未知轨迹，使用基于受试者步态和两种速度的条件化特征线性调制的Transformer去噪器。该模型仅在健全受试者数据上训练，在保留的健全受试者上实现了3.4°的平均绝对误差（MAE）。在未进行任何中风特异性微调的情况下，模型在训练分布外的中风受试者上实现了6.0°的MAE，保留了临床步态的受试者身份。相对于有监督的前馈基线，该框架将MAE降低了70%以上，且单个已知速度的精度与四个速度的精度相差在0.4°以内。这些结果表明，受试者条件残差扩散可以从最少的数据中合成跨速度的个性化步态，从而减轻下游外骨骼个性化的数据收集负担。

## Abstract
Personalizing exoskeleton assistance requires user-specific gait data across many locomotor tasks, yet collecting this data demands repeated motion capture sessions that are costly, time-intensive, and especially burdensome for clinical populations. This challenge is most acute across walking speeds, where gait changes substantially and deviates further in clinical gait. This work introduces a subject-conditioned residual diffusion framework that generates personalized lower-limb kinematics at unseen walking speeds from a subject's gait sequence at a single seen speed. Given sagittal-plane hip, knee, and ankle trajectories at a seen speed and a desired unseen speed, the model generates a residual that transforms the seen trajectory into the unseen one, using a transformer denoiser conditioned on the subject's gait and the two speeds through feature-wise linear modulation. Trained only on able-bodied data, the model achieved a mean absolute error (MAE) of 3.4° on held-out able-bodied subjects. Without any stroke-specific fine-tuning, it achieved a 6.0° MAE on out-of-training-distribution stroke subjects, retaining subject identity for clinical gait. The framework reduced the MAE by over 70% relative to supervised feed-forward baselines, and a single seen speed matched the accuracy of four speeds within 0.4°. These results demonstrate that subject-conditioned residual diffusion can synthesize personalized gait across speeds from minimal data, reducing the collection burden for downstream exoskeleton personalization.

---

## 论文详细总结（自动生成）

### 1. 核心问题与整体含义（研究动机和背景）

- **核心问题**：个性化外骨骼辅助需要用户在不同步行速度下的步态数据，但传统采集依赖耗时且昂贵的动作捕捉，对临床人群（如卒中患者）负担极重，严重阻碍外骨骼个性化部署。
- **整体含义**：本文提出一种**单源速度生成多速度个性化步态**的框架，利用生成模型（受试者条件残差扩散）从用户单一速度的步态序列中，合成其在其他速度下的下肢运动学，大幅降低数据采集成本，为外骨骼自适应控制提供高效方案。

---

### 2. 方法论：核心思想、关键技术细节、公式或算法流程

- **核心思想**：学习源速度步态到目标速度步态的**残差**（`r = x_tgt - x_src`），而非直接生成完整序列，以更好保留受试者身份特征。
- **关键技术细节**：
  - **模型架构**：基于 Transformer 的扩散去噪器，使用**特征线性调制（FiLM）** 将条件信息注入隐藏状态。
  - **条件嵌入**：
    - 受试者嵌入：通过双向 LSTM 对源步态 `x_src` 编码并 L2 归一化得到 `z_s`。
    - 速度嵌入：构造包含 `[v_src, v_tgt, v_tgt-v_src, v_tgt/(v_src+1e-6)]` 的向量，经 MLP 得到 `z_speed`。
    - 合并后通过线性层得到最终条件向量 `c`，再与扩散时间步嵌入相加。
  - **噪声调度**：标准方差保持扩散（1000 步），训练时预测噪声 `ϵ`。
  - **训练目标**：组合损失：
    - `L_noise`：加权噪声预测 MSE（膝关节权重加倍）。
    - `L_residual`：重建残差的 MSE。
    - `L_identity`：源=目标速度时鼓励零残差。
    - `L_subject`：对比损失，使生成序列的受试者嵌入与源序列嵌入相似。
  - **推理**：DDIM 采样 100 步，从高斯噪声去噪得到 `r`，恢复 `x_tgt = x_src + r`。

---

### 3. 实验设计：数据集、基准、对比方法

- **数据集**：
  - **健全受试者**（AB）：22 人，速度 0.5–1.85 m/s（步长 0.05 m/s）。
  - **卒中受试者**（Stroke）：19 人，速度 0.3, 0.6, 0.9, 1.2 m/s 及自选速度（仅用于评估，不参与训练）。
- **数据预处理**：每段 30 秒，降采样至 20 Hz（600 时间点），特征级 z-score 归一化。
- **基准方法**：
  - MLP：一对一映射（每对速度独立训练）。
  - TCN：从固定源速度到多目标速度的条件映射。
- **评估指标**：步态周期归一化 MAE、个人化排名（生成的轨迹与真实轨迹在人群中排序）、步频（cadence）。
- **对比实验**：
  - 扩散模型 vs MLP/TCN。
  - 不同源速度数量（1~4个）的影响。
  - 受试者编码 vs 随机编码（验证身份保持）。
  - 在全健和卒中受试者上的外推性能。

---

### 4. 资源与算力

- **文中未明确说明**使用的 GPU 型号、数量或训练时长（如小时数）。仅提到使用 AdamW 优化器，总训练步数最多 100,000 步，批大小 16，并使用了混合精度训练和梯度裁剪。因此无法量化具体算力消耗。

---

### 5. 实验数量与充分性

- **实验组数**：
  - 主实验：健全受试者 5 人测试，卒中 19 人测试；含整体 MAE、皮尔逊相关系数、个人化排名、步频。
  - 消融实验：源速度数量对精度的影响（1~4 个源速度，分析 MAE 差异）。
  - 速度配对分析：所有源-目标速度组合的热力图（健全和卒中）。
  - 个人化排名对比：受试者编码 vs 随机编码。
  - 基线对比（MLP, TCN）。
- **充分性判断**：实验覆盖了身份保持、速度泛化、跨人群泛化、与简单模型对比等关键维度，消融也较充分。但**缺少下游外骨骼控制任务的验证**，且卒中样本量有限（19人），泛化性证据仍需更大样本支持。

---

### 6. 论文的主要结论与发现

1. **性能优越**：健全受试者 MAE = 3.4°（r=0.95），卒中受试者 MAE = 6.0°（r=0.86），均优于基线（MLP 12.3°, TCN 11.4°），误差降低 >70%。
2. **单速度足够**：使用 1 个源速度 vs 4 个源速度的 MAE 差仅为 0.38°，表明数据采集可简化为单个速度。
3. **身份保持**：个人化排名在健全和卒中中分别为 3.3/22 和 2.8/19（越靠近1越保留身份），远优于随机编码（14.3 和 7.7）。
4. **临床泛化**：无需卒中数据微调，模型直接泛化到卒中受试者，且步频趋势正确。
5. **源速度鲁棒性**：各源速度下的 MAE 变化范围仅 0.41°，无需特定选择源速度。

---

### 7. 优点：方法或实验设计亮点

- **残差扩散设计**：生成增量而非完整序列，更易于保持受试者身份。
- **丰富条件注入**：速度嵌入含相对和绝对信息，FiLM 机制灵活调控隐藏状态。
- **多目标训练**：引入身份损失和受试者对比损失，防止向平均步态坍塌。
- **零样本临床泛化**：仅用健全数据训练即有效推广至卒中患者，显示强泛化能力。
- **实用导向**：单速度采样即可覆盖全速度范围，显著降低临床采集负担，具备实际部署潜力。

---

### 8. 不足与局限

- **样本限制**：卒中受试者仅 19 人，且速度范围有限（0.3–1.2 m/s），模型在更严重或更多样的临床人群上的表现未知。
- **缺失下游验证**：未评估生成的运动学对外骨骼控制策略（如数字孪生、控制器策略）的实际影响，MAE 不等于控制效果。
- **仅矢状面**：只考虑髋、膝、踝的矢状面角度，未包括额状面和横断面运动（卒中患者常有髋关节外展代偿）。
- **指标单一**：主要依赖 MAE 和个人化排名，未使用生物力学合理性（如地面反作用力、肌肉激活）或用户主观评价。
- **未明确算力**：缺乏对计算资源和训练成本的描述，妨碍可复现性和实际部署可行性评估。

（完）
