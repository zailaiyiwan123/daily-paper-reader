---
title: "PrefReward: Learning User Preference Matrix for Personalized Text Generation"
title_zh: PrefReward：学习用户偏好矩阵以进行个性化文本生成
authors: "Yue Wu, Chengbing Wang, Yimeng Bai, Xiaoyan Zhao, Yang Zhang, Fuli Feng"
date: 2026-07-23
pdf: "https://arxiv.org/pdf/2607.21067v1"
tags: ["query:pers-gen"]
score: 9.0
evidence: 通过用户偏好矩阵实现个性化文本生成
tldr: 针对现有个性化文本生成依赖隐式表征、难以解释用户偏好的问题，提出PrefReward框架。该方法通过提取用户偏好矩阵显式建模风格，并基于KL散度设计奖励函数指导解码过程。在LongLaMP数据集上，PrefReward在生成质量和可解释性上均优于非个性化和检索基线。该工作为个性化生成提供了显式偏好建模的新范式。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.21067v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 864, \"height\": 881, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.21067v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 871, \"height\": 528, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.21067v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 882, \"height\": 796, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.21067v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 848, \"height\": 530, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.21067v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 882, \"height\": 796, \"label\": \"Table\"}]"
motivation: 现有方法依赖隐式表征，可解释性差且难以处理长上下文依赖。
method: 提取用户偏好矩阵显式建模风格，并通过KL散度奖励函数引导解码。
result: 在LongLaMP数据集上生成质量和可解释性优于非个性化和检索基线。
conclusion: 显式偏好矩阵结合奖励信号有效提升个性化生成的准确性与可解释性。
---

## 摘要
大型语言模型（LLMs）通过利用用户历史记录和上下文线索，在生成个性化内容方面展现了卓越的能力。然而，现有的大多数个性化方法依赖于模型参数中的隐式表示，这使得难以解读用户特定的偏好或有效处理长上下文依赖。为了应对这些挑战，我们提出了PrefReward，一种新颖的偏好感知生成框架，通过结构化的偏好矩阵显式建模用户风格，并将其作为奖励信号整合到解码过程中。PrefReward包含两个阶段：（1）提取总结个体风格倾向的用户特定偏好矩阵，（2）通过基于KL散度的奖励函数，使用该矩阵指导生成。在LongLaMP数据集上的实验表明，PrefReward在生成质量和个性化可解释性方面均优于非个性化和基于检索的基线方法。

## Abstract
Large Language Models (LLMs) have demonstrated remarkable ability in generating personalized content by leveraging user histories and contextual cues. However, most existing personalization approaches rely on implicit representations within model parameters, making it difficult to interpret user-specific preferences or effectively handle long-context dependencies. To address these challenges, we propose PrefReward, a novel preference-aware generative framework that explicitly models user styles through a structured preference matrix and integrates it into the decoding process as a reward signal. PrefReward consists of two stages: (1) extracting a user-specific preference matrix that summarizes individual stylistic tendencies, and (2) using the matrix to guide generation via a KL-divergence-based reward function. Experiments on the LongLaMP dataset show that PrefReward outperforms non-personalized and retrieval-based baselines in both generation quality and personalization interpretability.

---

## 论文详细总结（自动生成）

# PrefReward：学习用户偏好矩阵以进行个性化文本生成 —— 详细论文总结

## 1. 核心问题与研究动机（背景）

- **问题**：现有的大语言模型（LLM）个性化生成方法大多依赖在模型参数内隐式编码用户偏好（如微调、适配器、prompt工程），导致 **可解释性差**（无法显式查看模型学到了哪些风格维度）且 **难以处理长上下文依赖**（检索到的历史可能噪声大、无法捕捉一致的风格）。
- **动机**：需要一种 **显式、可解释、无需额外训练** 的个性化范式，能清晰表达用户风格偏好并据此引导生成，同时保持计算轻量。

## 2. 方法论：核心思想与技术细节

- **核心思想**：通过构建 **用户偏好矩阵**（显式编码多种风格维度的概率分布），并在解码阶段使用 **KL散度奖励** 来挑选与用户风格最对齐的候选输出。
- **两阶段流程**：
  1. **偏好矩阵提取**：
     - **偏好标签集选择**：预定义一组可解释的风格标签（如 formal, concise, vivid, humor 等共41个维度），作为矩阵的列。
     - **检索器筛选**：用 BM25 从用户历史中选出最相关的 K 个 profile 样本（减少噪声）。
     - **Token级偏好提取**：对每个样本，利用预训练 LLM 的前向传播获得每个 token 的 logits，只保留属于偏好标签集的 token 的 softmax 概率，平均得到该样本的偏好向量 p_y。将 K 个样本的向量堆叠成用户偏好矩阵 P_u（K×N）。
  2. **偏好引导生成**：
     - 计算用户平均偏好分布 \(\bar{P}_u = \frac{1}{K} \sum p_{y_j}\)。
     - 对每个输入，用 LLM 生成 N_c 个候选输出，对每个候选提取其偏好向量 p_{y^{(i)}}。
     - 计算奖励 \(R_i = -KL(p_{y^{(i)}} \| \bar{P}_u)\)（KL散度越小，奖励越高）。
     - 采用 **Best-of-N** 解码：选择奖励最高的候选作为最终输出。

- **关键特点**：
  - 无需微调或强化学习，完全在解码时完成。
  - 偏好矩阵可直接可视化和比较，提供可解释性。
  - 利用已有前向计算的 logits，计算高效。

## 3. 实验设计

- **数据集**：LongLaMP 数据集（个性化长文本生成基准），聚焦于 **个性化评论写作任务**。每个用户随机采样 K=2 个 profile。
- **基准方法**：
  - NonPers（无个性化）
  - RAG (BM25) / RAG (CT)（基于检索增强）
  - CoS (λ=1.0, 2.0)（上下文导向解码）
  - PrefReward（本文方法）
- **骨干模型**：
  - Llama2-7B-Chat（零样本设置）
  - Gemma-2B-IT（经过对齐的模型）
- **评估指标**：ROUGE-1、ROUGE-L、METEOR（自动指标，衡量生成与参考的相似度）。
- **实现细节**：N_c=10 候选，nucleus sampling p=0.95，温度 τ=0.8。

## 4. 资源与算力

- **明确提到**：所有模型在 **NVIDIA A100 GPU** 上评估。
- **未说明**：具体的 GPU 数量、训练时长（因无需训练，仅推理）、总计算量。文中未提供这些细节。

## 5. 实验数量与充分性

- **实验组数**：
  - 主实验（表1）：在2种骨干模型上对比6种方法，共12个设置，每个设置报告3个指标。
  - 超参数探索（图2）：改变偏好标签集大小（从10到80），观察性能变化 —— 一组曲线。
  - 案例研究（图3）：一个定性示例，展示输出对比。
- **充分性与客观性**：
  - **优点**：对比了多种代表性基线（非个性化、检索、导向解码），覆盖零样本和对齐场景；分析了标签大小的影响；提供了定性案例。
  - **不足**：
    - **未进行消融实验**：例如检验 BM25 检索的必要性、Best-of-N 数量的影响、不同奖励函数（如余弦相似度）的对比。
    - **缺乏统计显著性检验**：未报告多次运行的标准差或显著性测试。
    - **仅一个数据集**：只在 LongLaMP 上测试，未扩展到其他个性化生成任务（如邮件、对话）。
    - **自动指标有限**：ROUGE 和 METEOR 对风格对齐不敏感，缺乏人工评估或更精细的风格一致性指标。

## 6. 主要结论与发现

- PrefReward **在所有指标上一致优于** 非个性化和检索基线，在 Llama2-7B-Chat 和 Gemma-2B-IT 上均取得最佳结果。
- RAG 和 CoS 在长上下文个性化中效果有限，因为检索噪声和缺乏稳定的风格建模。
- 偏好标签集大小存在 **收益递减** 现象：约20个标签可平衡表达与效率。
- 案例表明，PrefReward 能生成更贴近用户真实风格、更少幻觉的文本。

## 7. 优点

- **可解释性强**：偏好矩阵是显式的、人类可读的风格维度概率分布，可直观分析用户偏好，不同于隐式嵌入。
- **训练免费**：完全在解码阶段操作，无需微调或 RL，易于部署到任何预训练 LLM。
- **计算高效**：利用前向传播中已有的 logits，额外开销小。
- **模型无关**：在两个不同架构（Llama2、Gemma）上均验证有效。
- **方法新颖**：首次将用户偏好显式建模为矩阵并通过 KL 散度奖励引导解码，不同于检索或上下文导向方法。

## 8. 不足与局限

- **实验覆盖不足**：仅在一个数据集上测试，未涉及其他个性化文本生成任务（如摘要、邮件）；仅用一个骨干模型大小（7B 和 2B），未在更大模型（如 13B、70B）上验证。
- **指标局限**：ROUGE/METEOR 对风格匹配不敏感，且需要参考文本，可能不适用于创意生成；缺少人类评估或多样性指标。
- **偏好标签集的设计**：标签集是手动预定义的（41个），可能未涵盖所有用户风格；标签数量影响性能，但未提供自动化标签选择或用户自定义方案。
- **检索器依赖**：BM25 可能不是最优，且 K=2 的 profile 数量固定，未探索更多样本的效果。
- **Best-of-N 的计算成本**：生成 N_c 个候选会增加推理时间，可能不适合实时场景。
- **长期个性化未探索**：仅通过检索历史构建一次矩阵，未考虑随时间演化或跨 session 的用户风格变化。
- **公平性与偏差**：未讨论偏好标签选择可能引入的文化或语言偏见，例如某些风格（如“formal”）可能对不同用户群体有不同意义。

（完）
